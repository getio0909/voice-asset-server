# Program Risk Register

| ID    | Risk                                                                                                            | Likelihood | Impact   | Mitigation / evidence required                                                                                                                                                                 | Status                             |
| ----- | --------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| R-001 | Vendor ASR APIs drift, differ by region, or reject configured identities                                        | High       | High     | Implement from current official docs; retain sanitized fixtures; gate live tests by credentials; require a successful live result before claiming cloud verification. Tencent Flash now passes a credential-backed live WAV after active-key and account-APPID checks; Aliyun still requires live evidence | Partially mitigated; Tencent live, Aliyun open |
| R-002 | Android process death or network loss drops recordings                                                          | Medium     | Critical | Local file as source of truth, Room operation queue, WorkManager recovery, lost-response/process-restart JVM coverage, WAV repair plus validated MediaRecorder M4A duration/hash recovery, no-server startup regression test, and the StartupSyncPolicy gate that skips Profiles without a readable Keystore session at startup, after recording, Profile selection, and manual refresh; physical kill/reboot/network recovery still required | Open                               |
| R-003 | Transcript correction mutates or hallucinates source content                                                    | Medium     | Critical | Immutable five-stage lineage, structured patches, semantic validation, append-only review, approval, audit, and real-database E2E                                                              | Mitigated for Server Phase 3       |
| R-004 | Custom provider URLs enable SSRF or secret exfiltration                                                         | Medium     | Critical | HTTPS/host/IP policy, single validated DNS resolution with pinned dialing, mixed-address rejection, no redirects or ambient proxy, encrypted secrets, redacted logs                            | Application controls verified      |
| R-005 | Five independently versioned repositories drift                                                                 | High       | High     | Contract versions, capability negotiation, compatibility matrix, cross-repository E2E                                                                                                          | Mitigated through deployed 0.22 candidate |
| R-006 | Media processing permits argument injection or resource exhaustion                                              | Medium     | High     | Fixed FFmpeg argument construction, input limits, sandboxing, timeouts, media security tests                                                                                                   | Mitigated for bounded clips        |
| R-007 | Backup appears successful but cannot restore immutable objects                                                  | Medium     | Critical | Hash manifest, clean-instance restore tests, database/object consistency verification                                                                                                          | Mitigated through schema 18 restore drill |
| R-008 | Phase labels overstate unimplemented behavior                                                                   | Medium     | High     | Capability list and all five UIs/docs describe only tested behavior; distinguish local, deployed, fixture, and live evidence                                                                   | Mitigated through deployed 0.22    |
| R-009 | Android device-runtime behavior is unverified despite passing local builds                                      | Medium     | Medium   | Keep the installed Google SDK build gates green; 43 instrumentation methods compile, but the API 35 AVD reports missing AEHD and software launch produced no online adb device; run microphone, process-death, and network-recovery tests on an accelerated emulator or physical device | Open                               |
| R-010 | Site canonical deployment URL is undecided, so sitemap generation is disabled                                   | Low        | Medium   | Select the static host, set Astro `site`, and verify canonical URLs before public docs deployment                                                                                              | Open                               |
| R-011 | The isolated test host's managed PostgreSQL endpoint rejects that host's network path; the independent gateway also depends on renewed public certificate files | Medium | Medium | Keep `10443` test-only, use loopback PostgreSQL, retain restricted root-managed certificate symlinks plus the gateway-only daily reload timer, and retest the managed endpoint after its source allowlist changes | Mitigated for test deployment |
| R-012 | Remote Agent credentials outlive integrations or cannot be safely rotated                                       | Medium     | Critical | Hashed revocable keys, explicit scopes/expiry, separate inbound bearer, Agent audits, deployed Console lifecycle UI, and remote rotation/revocation tests                                      | Mitigated for test deployment      |
| R-013 | Expired Agent clips and exports accumulate until object storage fills                                           | Medium     | High     | Bounded worker reaper, exact integrity checks, retry-safe deletion, system audits, backlog monitoring, real-database tests, and deterministic remote E2E                                       | Mitigated for test deployment      |
| R-014 | Control-plane success hides database, upload, search, storage, worker, or media performance regressions         | Medium     | High     | Retain strict-TLS, database asset/search, multipart local-storage, Mock Worker, full-hash audio, real FFmpeg clip/waveform, and isolated S3 lifecycle/clean-restore baselines; continue adding production-sized S3 performance budgets before publication | Partially mitigated; production-sized S3 budget open |
| R-015 | Permanent deletion removes metadata while bytes remain, crosses a tenant boundary, or is triggered accidentally | Low        | Critical | Owner-only post-trash request, exact UUID reconfirmation, If-Match, idempotency, hidden purging state, storage-first integrity deletion, inventory fingerprint, bounded retry/resume, retained audits, real-PostgreSQL tests, and mocked accessible Console flow | Mitigated for test deployment      |
| R-016 | Notification delivery leaks another user's job data, copies sensitive payloads, or enables webhook SSRF          | Medium     | Critical | Session-only personal feed, workspace/user-bound cursors, safe terminal-field projection, immutable rows, transactional trigger/backfill, API-key denial, read audit, plus signed, allowlisted, retry-bounded Webhook delivery with encrypted secrets and real PostgreSQL/deployment tests | Mitigated for deployed Webhook slice; monitor provider endpoints |
